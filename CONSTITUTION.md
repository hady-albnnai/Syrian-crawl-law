# 🛠️ دستور عمل مستودع الزاحف القانوني (Syrian-crawl-law Constitution)

> **هذا المستند هو النسخة المنقحة من دستور مشروع ميزان الأصلي (`lawyer-office2/CONSTITUTION.md` v6.2)، مُكيَّفة لطبيعة هذا المستودع: زاحف Python يجمع النصوص القانونية السورية ويستخرج موادها، لا تطبيق Flutter يعمل دون اتصال.**
>
> أُجري التنقيح في 2026-09-05 بعد فحص المستودعين فعلياً — لا بالافتراض — التزاماً بقاعدة الخارطة (§Phase 0): *«دستور عمل منقح بعد قراءة المصدر الأصلي، وليس بالافتراض»*. الوقائع المثبتة موثقة في `DELIVERY/DELIVERY-0-MATCHING-REPORT.md`.

---

## 🔄 دورة حياة التعديل والتطوير (Workflow)

يُمنع إغلاق أي مهمة أو إبلاغ المستخدم بالانتهاء إلا بعد استكمال الخطوات الثلاث بالترتيب:

### 1️⃣ المرحلة البرمجية (Coding Phase)

- تنفيذ التعديلات في ملفات Python (`*.py`) ومخططات قاعدة البيانات.
- التأكد من عدم وجود أخطاء صيغة/أنواع بمراجعة الكود المعدَّل وقراءة الملف كاملاً بعد كل تعديل.
- اختبار منطق الاستخراج (Article Detection، hierarchy، التنظيف) على Fixtures قبل أي زحف حي.

#### ⛔ بوابة الرفع (Pre-Push Gate) — **تختلف عن المستودع الأم عمداً**

في مستودع ميزان الأم، الحكم النهائي هو جهاز المستخدم (Windows) لأن الاختبار المحلي كان متعذراً.
**هنا الوضع معكوس: بيئة Python قابلة للفحص محلياً ورخيصة، فالوكيل مُلزم بما يلي قبل الرفع:**

| # | الفحص | كيف |
|---|---|---|
| 1 | مراجعة الكود المعدَّل سطراً سطراً | قراءة الملف كاملاً بعد كل تعديل (لا الكتلة فقط) |
| 2 | عدّ مواضع الاستعمال قبل الحذف | `git grep -n "الرمز"` وفحص **كل** نتيجة |
| 3 | **تشغيل الاختبارات المحلية فعلياً** | `pytest tests/ -m "not network"` (أو ما يعادلها حتى تثبيت pytest في التسليم 1) — لا يُعلن إنجاز دون نتيجة تشغيل حقيقية |
| 4 | مطابقة التعديل مع الطلب | ربط كل تعديل بالمشكلة التي حلّها، وعدم التعميم |
| 5 | الالتزام بدورة الحياة الثلاثية | Coding ← Documentation ← Deployment ثم رفع |
| 6 | قراءة بوابة CI بعد الرفع (عند إضافتها في التسليم 1) | لا تُعلَن الدفعة منجزة وCI أحمر |

**لا يُدّعى نجاح اختبار لم يُشغَّل، ولا رقم استخراج لم يُنتجه تقرير تشغيل.**

#### 🔍 قواعد التعديل الآمن (منقولة من المستودع الأم — دروس مدفوعة الثمن)

نشأت هذه القواعد من أخطاء فعلية في مستودع ميزان الأم، وهي لغة-محايدة وتطبق هنا حرفياً:

1. **قبل حذف أي رمز، أحصِ كل مواضع استعماله.** في المستودع الأم حُذف رمز اعتماداً على تحذير يخص دالة واحدة بينما كان مستعملاً في تسعة مواضع أخرى، وفشل CI.
   → الصيغة المعتمدة: `git grep -n "الرمز"` (تشمل الاختبارات والأدوات، لا المصدر وحده).

2. **قبل إضافة أي رمز عام، تأكد أنه غير معرّف.**
   → `grep -rn "الاسم"` قبل أي تعريف جديد على مستوى الوحدة.

3. **الاستبدال النصي يجب أن يشمل سياقاً مميِّزاً.**
   ⚠️ **مثال حي من هذا المستودع:** كتلة إعدادات الزحف مكررة مرتين حرفياً في `config.py` (`DELAY_MIN/DELAY_MAX/TIMEOUT/MAX_RETRIES/MAX_DEPTH` تظهر في موضعين). أي استبدال نصي أعمى سيصيب نسخة ويترك الأخرى — وهذا عيب قائم مُدرج في تقرير المطابقة يجب إصلاحه في التسليم 1، لا التعايش معه.

4. **بعد أي تعديل، افحص الملف كاملاً لا الكتلة المعدَّلة** — الكتل تنتقل بين المواضع.

5. **لا تُعمِّم إصلاحاً دون فحص نطاقه.** في المستودع الأم: تعميم فك التشفير على المرفقات المشفرة والقوالب غير المشفرة كان سيكسر القوالب. هنا النظير: لا تُعمَّم قواعد تنظيف HTML الخاصة بمنتدى phpBB على مصادر WordPress أو PDF دون فحص كل مصدر.

---

## 🚫 محظورات تقنية (خاصة بهذا المستودع)

- **لا** تترك `print` في كود الإنتاج — السجلات عبر logging موحد (يُثبَّت في التسليم 1؛ الاستثناء الوحيد المسموح مؤقتاً: مخرجات CLI الموجّهة للمستخدم).
- **لا** تضف إعدادات سحرية مبعثرة في الوحدات — مصدر الإعدادات الوحيد `config` (ثم `.env` مع `.env.example` في التسليم 1).
- **لا** تُولّد معرّفات مستقرة من الوقت. العلة المثبتة حالياً في `crawler.py`:
  ```python
  f"doc_{int(time.time())}"
  ```
  إعادة زحف الصفحة نفسها تولّد `doc_id` مختلفاً، والاعتماد على `INSERT OR IGNORE` مع `source_url UNIQUE` وحده يترك `lastrowid` غير موثوق عند التجاهل. المعرّف المستقر يُشتق من المحتوى/الرابط المُطبَّع (مثل `sha256`) لا من لحظة التشغيل. **هذا بند P0 في الخطة ومحظور دستوري.**
- **لا** تكسر idempotency: إعادة تشغيل الزاحف أو الحفظ مرتين يجب ألا تنشئ نسخاً مكررة من وثيقة أو مادة.
- **لا** تغيّر مخطط قاعدة البيانات دون: رفع نسخة المخطط + مسار ترحيل (migration) + تقرير مطابقة قبل/بعد. تعديل المخطط وحده لا يصل إلى القواعد القائمة.
- **لا** تتجاوز `robots.txt` في أي زحف يتعدى التطوير المحدود. `RESPECT_ROBOTS = False` الحالية حالة تطوير مؤقتة **يجب ألا تصل إلى زحف واسع** (بند P0 في الخطة).
- **لا** تبتلع خطأً في عملية حرجة بـ `except: pass` أو `except Exception: pass` ثم تُكمل كأن شيئاً لم يكن — الفشل يُسجَّل بسببه ويُرفع في تقرير الدورة.
- **لا** تُبلغ بنجاح عملية لم تُنفَّذ أو برقم لم يُقَس. كل «تم استخراج …» يجب أن يرفقه تقرير تشغيل يذكر: التاريخ، المصدر، طريقة العد، وحالة الجودة. *(الرقم المتداول «1600+ مادة» يبقى ادعاءً غير موثَّق حتى تسليم القاعدة أو تصديرها — قاعدة الخارطة §3.3.)*
- **لا** تضع أسراراً (Tokens، مفاتيح API) في Git أو السجلات، ولا قواعد بيانات تشغيلية أو HTML خاماً ضخماً في Git (`.gitignore` الحالي يحرس ذلك — لا تُضعفه).
- **لا** تعتبر النص المستخرج قانوناً نافذاً. النص مادة أرشيفية للبحث؛ حالة النفاذ والتعديل والإلغاء طبقة منفصلة تتطلب مراجعة بشرية.
- **لا** تشغّل زحفاً واسعاً أو متوازياً على مصدر قبل اختبار محدود ومراجعة النتائج.

---

## 🎯 مبدأ الدقة المتناهية (منقول حرفياً من الدستور الأم — موجبُه هنا أقوى)

كل مهمة تُنفَّذ بدقة متناهية وتحليل كامل مهما أخذت من وقت وجهد، **دون اختصار ودون حلول جزئية**. السببان الموجبان:

1. **سبب يخص المشروع:** الناتج هنا نصوص قانونية سيبني عليها المحامي — عبر ميزان — مذكرات ومشورات. مادة ساقطة، أو رقم مادة مُزاح، أو نص ملتصق، يسبب ضرراً قانونياً حقيقياً. الخطأ في الاستخراج أخطر من الخطأ في العرض.
2. **سبب يخص كفاءة العمل:** الحلول الجزئية تجبر صاحب المشروع على إعادة شرح السياق من الصفر. الحل الكامل من أول مرة أوفر دائماً.

### قواعد التطبيق العملي:
- **لا افتراضات حول مخطط قاعدة البيانات:** قبل أي تعديل يمس الجداول، يُتحقق من التناغم بين `database.py` (ثم Alembic/migrations) والقيود والفهارس.
- **تتبع كامل للتبعيات:** تعديل عقد البيانات (document/article contract) يُلزم تحديث: الحافظ (database)، الزاحف، المستخرج، المصدر (exporter إلى ميزان)، والاختبارات — في نفس المهمة.
- **معالجة جذر المشكلة لا عرضها:** الثغرات المجاورة تُذكر صراحة في التقرير ولا تُكتم.
- **الإنجاز الحقيقي فقط:** لا يُعلن اكتمال مرحلة إلا باكتمال سلسلتها (`Contract` + `Code` + `Tests/Fixtures` + `Documentation` + `Report` + `Git Push`).

---

## 🧱 المبادئ غير القابلة للتفاوض (مرفوعة من ROADMAP §4 إلى مرتبة دستورية)

1. **صحة المصدر والتتبع:** كل وثيقة ومادة ترتبط برابط المصدر، وقت الجلب، بصمة المحتوى، إصدار أداة الاستخراج، وحالة جودة. لا إجابة RAG مستقبلاً بلا رجوع إلى الوثيقة والمادة.
2. **عدم التكرار وقابلية إعادة التشغيل:** حفظ idempotent ومعرّفات مستقرة.
3. **الزحف المهذب:** robots.txt فعلياً، User-Agent واضح، تأخير قابل للضبط، تسجيل حالات المنع.
4. **البيانات القانونية ليست بديلاً عن المراجعة القانونية:** فصل النص الخام ← المنظف ← المستخرج آلياً ← المراجَع بشرياً.
5. **حماية الأسرار:** `.env` محلي، `.env.example` بلا أسرار، تنقية السجلات.
6. **الاختبار قبل التوسعة:** كل تغيير مؤثر في extractor/crawler يملك Fixtures واختبارات محلية، وشبكة منفصلة اختيارية.

---

## 🧠 مبدأ الجاهزية للذكاء الاصطناعي (منقح عن الدستور الأم)

في المستودع الأم النمط: **قالب ← نسخة مخصصة** مع سجل تفاعل كامل للتدريب.
نظيره هنا: **وثيقة ← مواد ← نسخ زمنية** — كل مادة وحدة بيانات تدريبية قابلة للتتبع:

| القاعدة | التفصيل |
|---------|---------|
| **لكل مادة بصمة ومصدر** | `article_key` مستقر + `source_locator` (url + html_hash) |
| **ميّز حالة المراجعة** | `review_status`: `auto_extracted` / `needs_review` / `human_verified` |
| **سجّل الجودة لا تخفِها** | `quality_score` مع سبب الرفض عند الفشل — لا إسقاط صامت |
| **النسخ الزمنية لا الاستبدال** | تعديل النص المستخرج = نسخة جديدة (`article_versions`)، لا كتابة فوق القديمة |
| **اربط الإصدارات بالمتجهات** | كل embedding يرتبط بنسخة مادة لا بالمادة المجردة |
| **لا تمسح الأصل** | HTML الخام/bypass snapshots تُحفظ (خارج Git) للتدقيق وإعادة الاستخراج |

---

## 🔗 عقد التكامل مع ميزان (`lawyer-office2`)

### الوقائع المثبتة (2026-09-05):
- ميزان تطبيق **Flutter لسطح مكتب Windows، Offline-First**: Drift/SQLite محلية، Riverpod، بلا أي خادم خلفي.
- يستهلك المحتوى القانوني كـ**حزمة ملفات**: `content/legal_library/laws_decrees/` فيها فهرس `laws_decrees_index.csv` (29 قانوناً: id, title, type, number, year, date, category, url, format, priority, status, local_path, size_bytes, **sha256**) + ملفات PDF + مستوردات HTML/JSON.
- جدول `LegalLibraryItems` في Drift هو نقطة الاستهلاك داخل التطبيق (itemType, title, category, sourceUrl, filePath, extractedText, lawNumber, lawKind, court, chamber...).

### الاتجاه المعماري (الخيار 3 الانتقالي — مُوصى به، بانتظار اعتماد المالك):
يبقى هذا المستودع Python مستقلاً، ويُبنى **مُصدِّر (Exporter)** يحوّل مخرجاته إلى حزمة ملفات مطابقة لعقد `laws_decrees_index.csv` الحالي، فتُستهلك في ميزان دون تغيير مخططه. عقد البيانات الغني (documents/articles/versions) يبقى الأساس للبحث الدلالي وRAG لاحقاً إن قررت المنصة التحول إلى خادم.

**لا يُحسم خيار Monorepo أو الخدمة المستقلة الدائمة إلا بقرار المالك بعد مراجعة تقرير المطابقة.**

---

## 📄 المرحلة التوثيقية (إلزامية)

- تحديث `DELIVERY/PLAN-CHECKLIST.md` بعد كل تسليم (نقل البند من `[ ]` إلى `[x]` فقط عند التحقق الفعلي من وجود مخرجه).
- تحديث `docs/ROADMAP.md` عند أي قرار معماري أو انحراف عن الخطة.
- كل رقم يُذكر عن البيانات (أعداد مواد/وثائق) يرفقه تقرير يذكر طريقة الحساب وتاريخها ومصدرها.
- القرارات المؤجلة تُسجَّل في تقرير المطابقة «قرارات مفتوحة» — **لا تُحسم بالافتراض**.

## 🚀 مرحلة الرفع (إلزامية)

```bash
git add .
git commit -m "وصف دقيق للتعديلات المنجزة ومطابق للخطة"
git push origin main
# تأكيد الوصول:
git fetch origin && git log --oneline origin/main -1
```

`git push` الناجح لا يعني وصول التعديل — يُتحقق من `origin/main` مباشرة (درس مدفوع من المستودع الأم).

---

## ⏸️ ما لا ينطبق من الدستور الأم (مؤجَّل حتى Phase 3)

- **الهوية البصرية** (كحلي + ذهبي، GlassmorphicCard...) و**هندسة القائمة الجانبية**: تخص واجهة Flutter — تُستعاد عند بناء واجهة ميزان على الـ API (Phase 3)، وتبقى مرجعيتها الدستور الأم.
- **المعالج الشامل لتسجيل النتائج** وقواعد Drift/`schemaVersion`/`build_runner`: تخص التطبيق الأم، وتبقى نافذة هناك كما هي.

---

**تذكير ذاتي:** «العمل غير الموثق وغير المرفوع هو عمل غير موجود — وكل رقم بلا تقرير مصدر لم يُقل.»
