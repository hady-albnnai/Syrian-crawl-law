# Databases
*.db
*.sqlite
*.sqlite3
data/*.db

# Temporary outputs
export/
dist/
build/
recon_output/
logs/
output/

# لقطات HTML الخام التشغيلية فقط — لا Fixtures الاختبار (tests/fixtures/*.html)
# كانت `*.html` عامة تحجب fixtures الاختبار عن Git فيفشل CI في collection
# (عطل مثبت 2026-09-05: 16 تشغيلة CI متتالية فاشلة لهذا السبب بالضبط).
data/snapshots/*.html

# Python
__pycache__/
*.pyc
*.pyo
venv/
env/
.env

# OS
.DS_Store
Thumbs.db

# Editors
.vscode/
.idea/

# artifacts البناء والتثبيت — كانت mizan_harvester.egg-info متتبَّعة في Git
# فتتغيّر مع كل `pip install -e .` وتلوّث git status بمحتوى مولَّد (فُصلت هنا).
mizan_harvester.egg-info/
*.egg-info/
.pytest_cache/

# ف٣: تقدّم محرك damascusbar (بيانات تشغيلية)
data/damascusbar_done.json

data/cdx_years/
