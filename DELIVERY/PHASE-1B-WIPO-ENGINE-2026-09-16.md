# تقرير ف١-ب — مصدر ويبو ليكس: أول إسناد رسمي كامل (قانون العقوبات)

**التاريخ:** 2026-09-16 — **المدى:** ف١-ب من ترتيب الفتح المعتمد بتقرير
ف٠ §1 (قرار المالك «موافق ع اقتراحاتك»).

## 1. البوابة المقابلة

بوابة ف١ بالميثاق: «قانون كامل بإسناد جريدة/مصدر معتمد + سلسلة تعديل +
حالة قانونية، محروس باختبارات بنتيجة تشغيل». بهذه الدفعة: **قانون
العقوبات (المرسوم التشريعي 148/1949) — 775 مادة — بإسناد إيداع رسمي
دولي (ويبو ليكس، تير 2)**، والسلسلة تكتمل عند المالك آلياً: قاعدته تحوي
3 وثائق تعدّل هذا الصك تحديداً (قِيست `--list` على جهازه)، فحالته
تصبح «معدَّل» بسلسلة فور دخوله.

## 2. ما بُني

| المكوّن | الملف | الوصف |
|---|---|---|
| مصدر ويبو | `wipo_source.py` | صفحة التفاصيل ← استخراج رابط الـPDF الموقّع من الـiframe (فك كيانات HTML قياسياً) ← جلبه بأدب كامل (robots + تأخير fetcher + ترويسة Referer) ← استخراج PyMuPDF ← تطبيع ← HTML مصنّع بنفس بنية أسطر المصدر ← **نفس بوابات الأنبوب بلا مسار خاص** |
| خطّاف الدورة | `crawler.py` | مهام صفحات تفاصيل ويبو تُحوَّل قبل المعالجة؛ فشل التحويل = فشل مهمة نظيف |
| هوية بصيغة الإسناد المائلة | `law_identity.py` | «رقم 148/1949» (صيغة ويبو) بجانب «رقم 17 لعام 2010» — بمجموعات مسماة |
| تير الإسناد | `config.py` | `wipo.int: 2` موثق السبب (إيداع رسمي دولي) |
| بذر | `official_seed.seed_wipo` + `WIPO_SEED_LAWS` | العقوبات أولاً — القائمة تتوسع بف٢ بفهرسة الدولة |
| إصلاح قصّ المتن | `crawler.py` + `MAX_CLEAN_CONTENT_CHARS` | كان 15000 حرفاً ضمنياً — قصّ العقوبات إلى 8٪؛ الآن 2M موثقاً |
| تبعية | `pyproject.toml` | `pymupdf` + إكمال py-modules الناقصة تاريخياً |

## 3. أخطاء حقيقية اكتُشفت وأُصلحت بالقياس (سجل التعلم)

1. **كيانات HTML بالرابط الموقّع** (`&#x3D;`): الرابط يُرسل مكسوراً
   فيرفضه CloudFront بـ403. اكتُشف بالزحف الحي فقط — الاختبارات المحقونة
   لا تمر من الشبكة فلم تمسكه (لهذا أُضيف اختبار صريح للكيانات).
   الإصلاح: `html.unescape` قياسي.
2. **انعكاس خانات الأرقام بصرياً**: تسلسل المواد 166-170 يظهر خاماً
   661/761/861/961/071 — كل سلسلة أرقام متعددة الخزانات مخزنة معكوسة؛
   تُقلب خانةً خانة. الدليل: التسلسل يستقيم بعد القلب.
3. **نمط ضجيج المنتديات يبتلع البلوك المدموج**: `-{10,}.*?$` مصمم لمسح
   سطر توقيع؛ لما دمجت أسطر الـPDF بفقرات طويلة صار أول خط شرطات زخرفي
   يمسح كل ما بعده بالسطر (792 مطابقة ← 133!). الحل الهيكلي: كل سطر
   مصدر = عنصر مستقل — بنية الأسطر محفوظة فيبقى سلوك الأنبوب كما صُمم.
4. **ترويسة الصفحة وأرقامها**: ترويسة (عنوان التشريع) ورقم الصفحة
   يتكرران بكل صفحة — يُحذفان؛ و«المادة» المتبوعة بسطر أرقام: أولها
   رقم المادة والباقي صفحات.
5. **قصّ clean_content عند 15000** (خلل قديم سابق للمصادر الرسمية):
   نص 195k حُفظ 15k. الإصلاح موثق أعلاه.
6. مكتبة PDF بالقياس: **PyMuPDF 810 أنماط مادة مقابل 24 لـpypdf** —
   القرار بلا جدال.

## 4. بوابات الجودة — نتائج تشغيل حقيقية

```
$ python3 -m pytest tests/ -q
241 passed in 4.00s     (221 سابقة + 20 جديدة — محلية بلا شبكة)

$ python3 cli.py crawl --mode limited --pages 1     # زحف حي فعلي من الشبكة
   ✅ حُفظ القانون الجنائي (المرسوم التشريعي رقم 148/1949) — 775 مادة | 0 إخفاقات

وثيقة العقوبات بالقاعدة:
  identity_key = المرسوم التشريعي:148:1949 | tier = 2 | فرع = جزائي
  legal_score = 80 | جودة = 0.9 | مسارات هرمية = 775/775
  clean_content = 192,895 حرفاً (كامل بعد إصلاح القص)
  الحالة = ساري (بقاعدة الصندوق — عند المالك: معدَّل بسلسلة 3)

دقة موثقة بلا مبالغة: 684 رقم مادة فريد، المدى 1→756، فجوات تسلسل
متناثرة (أكبرها 5؛ المواد «الغائبة» موجودة بالنص الخام — إما جسم قصير
استبعده المستخرج عمداً أو إحالة داخلية سبقت رأس المادة).
```

## 5. الطقم الذهبي الجديد

`tests/fixtures/wipo/penal_code_148_1949.pdf` (122 صفحة) +
`penal_details_ar.html` — أي تغيير مستقبلي بالمستخرج أو التنظيف يُقاس
عليهما فوراً (اختبار `test_golden_pipeline_articles_and_quality`
يشترط ≥700 مادة ويشمل الأرقام التي كانت معكوسة).

## 6. على جهاز المالك (لإقفال بوابة ف١ كاملة)

```
git pull
python cli.py tasks --contains wipo
python cli.py requeue --status success --contains legislation/details
python cli.py crawl --mode limited --pages 3
python cli.py law-status --rebuild --list
```

سطر `tasks` يكشف حالة مهمة ويبو وآخر عطلها (التشخيص قبل العلاج). سطر
`requeue` هنا يستهدف `success` تحديداً: مهمة العقوبات عند المالك
معالَجة قديماً بحالة success (انظر ٦-ج) — والرشح `legislation/details`
يصيبها وحدها دون روابط قوائم ويبو. المتوقع بعد الـcrawl: **ترقية صف
العقوبات في مكانه** إلى نسخة الـPDF الرسمية (775 مادة، تير 2) — انظر
٦-د لماذا «في المكانه» ولماذا يبقى عدد الوثائق كما هو. وبالسطر الأخير:
`تعديل ×3 ← المرسوم التشريعي:148:1949 (✓ موجود بالمتن)` وحالته
**معدَّل** — بوابة ف١ بالنص الحرفي للميثاق.

### ٦-أ. ملاحظة نشر — أول تشغيل عند المالك كشف فجوة عتاد (أُصلحت)

أول تشغيل حقيقي عند المالك (2026-09-16) كشف: استيراد pymupdf كسول
بـ`pdf_to_text`، فمحاولة زحف قبل اكتمال `pip install` تصل لحد التحويل
ثم تفشل المهمة — والبذر يتخطى الرابط الموجود **أياً كانت حالته**
والطابور لا يلتقط الفاشلة ثانية، فتبقى المهمة ميتة بلا سبيل للعودة
(العَرض: «بذر ويبو: skipped 1» + «الطابور فارغ»). الإصلاح: أمر
`requeue` (منطقه `crawl_queue.requeue_by` — يعيد بالحالات المطلوبة
مع تصفير عدّاد المحاولات، ورشح `--contains`، ولا يمسّ needs_review
إلا بطلب صريح) + 3 اختبارات. عتاد قياسي لطابور دائم سيلزم ف٢ كثيراً.

### ٦-ب. جولة المتابعة — التشخيص اكتمل وثلاث أراضٍ أُزيلت (2026-09-16 مساءً)

تشغيل المالك للمرة الثانية حسم الحقيقة: **السلسلة تعمل فعلاً** —
`تعديل ×3 ← 148:1949 (✓ موجود بالمتن)` وحالة `معدَّل: 1`، لأن نسخة
منتدى لقانون العقوبات كانت مخبأة بمتنه بلا هوية (صيغة «148/1949»
المائلة كانت خارج النمط القديم) فكشفها `--reidentify` بالصيغة الجديدة
(gained 2). ومهمة ويبو ليست failed (requeue لم يجدها) — القصة
المكتملة من الكود: محاولتها الأولى ماتت قبل بلوغ الـPDF (فشل جلب
صفحة التفاصيل صُنّف حجباً ← هي على الأرجح الـblocked الوحيدة بالطابور).
وفي أثناء التشخيص أُزيلت ثلاث أراضٍ:

1. **قنبلة SystemExit بpdf_to_text**: غياب المكتبة كان يرفع SystemExit
   (BaseException لا يلتقطه except Exception) — يقتل الدورة فوراً
   ويترك المهمة عالقة running أبداً. صار RuntimeError محتوى بـ
   as_pipeline_result: فشل مهمة نظيف برمز `wipo_pdf_module_missing`.
2. **لا إنقاذ للمهام العالقة**: أي دورة تنكسر (انقطاع/Ctrl+C/استثناء)
   تترك مهمتها running ميتة — claim_next لا يراها. أُضيف
   `requeue_stale_running` ببداية كل دورة (مهلة 10 دقائق تحمي دورية
   الويب المتوازيتين من سرقة مهمتها النشطة).
3. **عمى الطابور**: لا سبيل لرؤية حالة مهمة معينة — هذه الجولة كلها
   حدثت لغيابها. أُضيف أمر `tasks` (رشح بالحالة والرابط، مع المحاولات
   وآخر عطل).

249 اختباراً ناجحة (244 + 5 جديدة).

### ٦-ج. الجولة الثالثة — `tasks` حسمت اللغز: المهمة success قديمة من عصر الاكتشاف

تشغيل `tasks --contains wipo` عند المالك (2026-09-16 مساءً) كشف الحقيقة
كاملة: مهمة العقوبات **ليست فاشلة ولا محجوبة — بل success معالَجة
قديماً كمهمة قسم (section)**. القصة: بجلسة اكتشاف سابقة (قبل بناء
محرك ويبو) كشف `discover` صفحة تفاصيل العقوبات، وتقييمها (تير 2 —
إيداع رسمي دولي) حكم عليها `recommended` فاعتُمدت مصدراً تلقائياً
باسم عنوانها الحرفي، والمصادر المعتمدة تُبذَر كل دورة كمهام section —
فزحف ما قبل المحرك عالجها صفحةَ قائمة: روابط قوائم موقع ويبو
(madrid-system، hague-system…) صارت مهام topic كلها needs_review
(قوائم موقع لا وثائق — ضجيج بريء يُرفض بالمراجعة البشرية)، والمهمة
نفسها نجحت بلا حفظ (لا مواد بصفحة التفاصيل — النص داخل PDF).
فلم ينفع أي `requeue` بحالات ميتة دون success، وبذر ف١-ب وجد الرابط
موجوداً فتخطاه. العلاج سطر واحد: إعادة المهمة success الوحيدة
برشح `legislation/details` — الخطّاف يعمل لأي kind، ومسار section
يكشف «الصفحة نفسها وثيقة كاملة» فيحفظها.

## 7. التالي بالترتيب المعتمد

ف٢ الكتلة الكبرى: pministry (مجلدات التشريعات من جهاز المالك — TLS) +
dig-doc (الجريدة المرقمنة) + فهرسة ويبو كاملة + أرشيف parliament عبر
HF/Wayback ← ف٢-ج الطبقة المجتمعية (syria-law 437 رابطاً) ← ف٣
الاجتهادات.

### ٦-د. الجولة الرابعة — «محفوظة سابقاً» كشفت تصميماً أعمى بالمحتوى (أُصلح)

إعادة مهمة العقوبات نجحت والمحرك اشتغل عند المالك بالكامل (تحويل ✓
نص 195k ✓ «الصفحة نفسها وثيقة كاملة (775 مادة)» ✓) ثم اعترض الطريق:
**«🔁 الوثيقة محفوظة سابقاً — تخطي بلا تكرار»**. التشخيص بالمحاكاة
على الطقم الذهبي: **صفحة تفاصيل ويبو الخام تحمل نص القانون كاملاً في
متنها (190 ألف حرف)** — فالزحف القديم (قبل الخطّاف، عصر الاكتشاف)
اجتاز بها كل البوابات وحفظها وثيقة بـ11 مادة من رابط ويبو نفسه. هذا
الصف القديم يسدّ الطريق: فحص `doc_id` (sha256 الرابط — ضمانة عدم تكرار
الجلب) كان أعمى بالمحتوى: لا يفرق بين إعادة زحف بنفس المحتوى (تكرار
حقيقي → تخطٍّ صحيح) وإعادة زحف بمحتوى أفضل (المصدر تطوّر → يجب أن
يدخل). الإصلاح: إعادة زحف نفس الرابط بمحتوى مختلف تمر بنفس ميزان
dedup — إن كانت الجديدة أفضل (تير/اكتمال/مواد/هرمية/طول) تُؤرشف
القديمة (نسخ لا حذف، document_versions) ويُحدَّث الصف في مكانه
(doc_id مستقر) وتُستبدل مواده؛ إن كانت أسوأ أو مطابقة يبقى التخطي
كما صُمم. ثلاثة اختبارات: ترقية المالك حرفياً (خام 11 مادة ← PDF
775 في المكان + أرشفة)، وحارسا الاتجاهين (مطابقة → تخطٍّ، أسوأ →
يبقى الأفضل). 252 اختباراً ناجحة.

**علامة النجاح عند المالك بعد الترقية:** سطر `♻️ استُبدلت بنسخة أفضل
من نفس المصدر (...)` بالدورة، وعدد الوثائق يبقى 62 (تحديث في المكان)
بينما تقفز المواد من 4,022 إلى ≈4,786 (4,022 − 11 القديمة + 775).

### ٦-هـ. الإقفال الرسمي — تشغيل المالك النهائي (2026-09-16)

كل العلامات تحققت حرفياً: `♻️ استُبدلت بنسخة أفضل من نفس المصدر
(domain_tier)` ← `✅ حُفظ القانون الجنائي (775 مادة)` ← القاعدة
62 وثيقة | **4,786 مادة** (التنبؤ 4,786 = 4,022 − 11 + 775).
**بوابة ف١ مقفلة بالنص الحرفي للميثاق:** قانون كامل (775 مادة) بإسناد
إيداع رسمي دولي (تير 2)، بسلسلة تعديل (3 وثائق معدِّلة)، وحالة قانونية
(معدَّل) — محروس بـ252 اختباراً وبطقم ذهبي. المتبقي بالطابور للمراجعة
البشرية المتراكمة: blocked 1 + failed 2 (moj قديمة content_too_short) +
needs_review 51 (أغلبها قوائم موقع ويبو البريئة).

---

## 8. ف٢ انطلاقة — فهرسة ويبو الكاملة (صفحة عضوية سوريا)

**الفهرس الحي:** `config.WIPO_INDEX_URL` = صفحة عضوية سوريا بويبو ليكس —
تعرض كل الصكوك المودعة بروابط تفاصيلها (قِيس 2026-09-16: 22 صكاً).
`seed-official` يقرؤها حياً بكل بذر: إيداع جديد يظهر تلقائياً بلا تحديث
يدوي. المثبتات القديمة تبقى احتياطاً إن تعذّر الفهرس.

**المصدران المرشحان (القياس قلب الافتراض):** قياس الدفعة كشف أن بعض
الإيداعات مسح ضوئي بلا طبقة نص (جمارك: 78 حرفاً من 2.4MB) أو بخطوط
بترميز مخصص تخرج خردة محارف تحكم (مدني/عمل/منافسة/آثار) أو معكوسة
بصرياً (دستور) — بينما متن صفحة التفاصيل نفسها نظيف وكامل عند بعضها،
والعكس عند العقوبات (متن 11 مادة مقابل PDF بـ775). فالتحويل اليوم يبني
المرشحَين ويطبق بوابات الصلاحية (طول ≥1000، عربية ≥25٪، خردة <5٪،
مواد ≥1 للـPDF و≥5 للمتن — بوابة البويلربليت) ويفوز الأكثر مواداً،
والتعادل للـPDF (الإيداع الرسمي). **أعطال الجلب (robots/غلاف/غياب
مكتبة) تفشل المهمة لتُعاد ويُجلب الإيداع كاملاً — البديل يقتصر على
الرداءة الجوهرية.**

**النتيجة المقيسة (زحف حي كامل من الصندوق — 18 صكاً محفوظاً):**

| الصك | المصدر | مواد |
|---|---|---|
| العقوبات 148/1949 | PDF | 775 |
| التجارة 33 | PDF | 651 |
| الجمارك 38/2006 | صفحة التفاصيل | 369 |
| تعليمات ضريبة الدخل 24/2003 | PDF | 276 |
| ضريبة الدخل 24/2003 | PDF | 198 |
| الدستور 2012 | صفحة التفاصيل | 150 |
| حق المؤلف 62/2013 | PDF | 114 |
| براءات الاختراع 18/2012 | PDF | 103 |
| لائحة العلامات الفارقة | PDF | 84 |
| التجارة الداخلية 14/2015 | PDF | 69 |
| العلامات الفارقة | PDF | 62 |
| تعليمات التواصل على الشبكة | PDF | 46 |
| الإعلام 108/2011 | PDF | 45 |
| التطوير والاستثمار العقاري 15/2008 | PDF | 45 |
| رسوم البراءات 28/1980 | PDF | 39 |
| التشاركية 5/2016 | PDF | 8 |
| مرسوم 17/2012 (تطبيق التواصل) | PDF | 7 |
| قانون 17 لعام 2019 | PDF | 6 |

و4 إخفاقات صادقة برمز واحد صريح `wipo_pdf_junk_text`: **المدني
84/1949** (الإيداع بخطوط بترميز مخصص — متن صفحته بويلربليت لا ينقذه)،
العمل 17/2010، المنافسة 7/2008، الآثار 222/1963 — رداءة بويبو نفسها
لا عندنا؛ المدني هدفه الأساسي مصادر ف٢ الأخرى (pministry/dig-doc).
سلاسل الإحالات تنمو تلقائياً: جمارك سجلت 2، وتعليمات ضريبة الدخل
سجلت 1 (قِيس بالصندوق).

**على المالك:** `git pull` ثم `seed-official` ثم `crawl --pages 25` —
المتوقع +17 وثيقة تير 2 (~3,047 مادة) و4 إخفاقات صادقة، ثم
`law-status --rebuild --list`. 260 اختباراً ناجحة.

### ٨-أ. المالك أكمل دفعة ويبو كاملة — والحالة القانونية أزهرت

تشغيل المالك (2026-09-16 مساءً): +17 وثيقة / 2,504 مادة ← **79 وثيقة |
7,290 مادة**، و4 إخفاقات صادقة (`wipo_pdf_junk_text`)، والبذر الثاني
أثبت الـidempotency (أُضيف 0). المفاجأة القيّمة: محرّك الحالة أزهر —
**22 إحالة** (كانت 14) و**ملغى 2 حقيقية** (القانون 12/2001 والمرسوم
التشريعي 50:2001 — كلاهما بالمتن ومُلغى بصكوك أخرى) وساري 42. النظام
يقول الآن «هذا القانون لم يعد سارياً» بذاته.

### ٨-ب. dig-doc.org محسوم (بند ف٠ أُغلق بمعلومة مقيسة)

الاستكشاف الحي (الصندوق، 2026-09-16): «دار الوثائق الرقمية التاريخية»
(أرشيف المحامي علاء السيد حلبي) — الجريدة الرسمية 1935-1944 والنشرة
الرسمية 1930-1932 معروضة وصفياً و**المجلدات الرقمية بالطلب يدوياً عبر
المراسلة** (لا روابط تحميل مباشرة بالموقع إطلاقاً)، و«إصدارات حكومية»
سالنامات عثمانية. لا قيمة زحف آلي لف٢ — يبقى مرجعاً تاريخياً إن
احتاجته المكتب يوماً بالطلب. robots متساهل (Disallow: /admin فقط).

### ٨-ج. pministry — المسبار جاهز لجهاز المالك

الوصول من الصندوق ميت (فشل مصافحة TLS بكل الخيارات القديمة — مثل ف٠).
أُضيف `tool/probe_pministry.py`: جلسة TLS متساهلة (SECLEVEL=1 بلا
تحقق شهادة)، يفحص HTTPS/HTTP وrobots ويرسم خريطة روابط التشريعات
المرشحة من الرئيسية. يعمل من جهاز المالك:
`python tool\probe_pministry.py` ثم لصق الخرج هنا — المحرك يُبنى
على بنية مقيسة لا تخمين. (مُختبر حياً على dig-doc من الصندوق.)

### ٨-د. مجموعة HF (endomorphosis/ipfs_syria_laws) — نُزّلت وفُحصت (قرار ف٢ التنفيذي معلّق للمالك)

**الحالة المقيسة (الصندوق 2026-09-16):** pministry معلّق عالمياً (nginx
يرد 301 على HTTP كله نحو HTTPS؛ الاتصال 443 يفتح ويستلم الشهادة
(*.pministry.gov.sy) ثم يتجمد للأبد — نفس السلوك عند المالك والصندوق؛
كان يعمل وقت ف٠ فالغالب عطل مؤقت — المسبار جاهز لإعادة الفحص).
Wayback مطفأ مؤقتاً («Temporarily Offline» — أرشيف CDX غني بالموقع:
الرئيسية 2014 + مئات لقطات صفحات ?id= حتى 2025). مجموعة HF نزلت
كاملة (sha256 مطابق للمانيفست بالبايت) وفُحصت:

- **174 صكاً / 7,179 مادة** — مسح أرشيفي لصفحات parliament.gov.sy
  الأصلية (+ بعض PDFات jus.moj) — source_urls أصلية = فهرس اكتشاف جاهز.
- **جواهر جديدة عنا:** أصول المحاكمات 1953 (547 مادة)، أصول المحاكمات
  الجزائية 1950 (457)، **الأحوال الشخصية 59 (307)**، السير (217)،
  العقوبات العسكري 61:1950 (172)، البينات (157)، السلطة القضائية
  98/1961 (138)، التأمينات 92:1959 (134)... — التقاطع مع متن الصندوق:
  6 هويات فقط.
- **جودة غير متجانسة:** 63 صكاً بمواد صفرية (فشل مستخرجهم — أغلبها
  PDFات)، ومرض انعكاس الأرقام ظاهر بعناوين صفوف MoJ («0212»=2012)
  — نفس أرض ويبو؛ بواباتنا (عربية/خردة/مواد) تميزها.
- هوية كودنا التقطت 103 من 174 (عناوين بعض الجواهر بلا رقم/سنة واضح).
- المدني 84/1949 ليس بها (بقي هدفاً عبر pministry/أرشيفه).

**الخيارات (قرار ف٠ التنفيذي، يعرض على المالك):** (أ) فهرس اكتشاف
حصراً — زحف الأصول المؤرشفة عند عودة Wayback (إسناد مصدر أول، أبطأ)؛
(ب) تبنٍّ مرحلي محروس — الـ111 صكاً ذات المواد يدخلون أنبوبنا ببوابات
الجودة الآن مع تحقق لاحق ضد الأصول؛ (ج) الاثنان معاً — التبني المرحلي
الآن وبناء زحف الأصول، وعند اكتماله يفوز الأفضل بآلية dedup الموجودة.
الملفات محفوظة خارج git بoutput/hf_ipfs/.

### ٨-هـ. تنفيذ قرار ج — التبني المرحلي لمجموعة HF عبر بوابات الأنبوب (مبني ومقيس حياً)

**البناء:** `hf_syria_laws.py` + أمر `cli hf-import` + pyarrow بالتبعيات.
التنزيل يتحقق sha256 ضد مانيفest المجموعة (بصمات بقاعدة config). كل صك
جاهز يمر **بنفس `_handle_topic` بكل بواباته** — لا مسار خاص: استخراج،
فحص قانوني، بوابة جودة، هوية، منقّح dedup، سلسلة إحالات. الإسناد
الصادق: طبقة 3 تُمرَّر بالمهمة صراحة (نص رسمي منقول بمجموعة مجتمعية —
نفس منطق ar.wikisource)؛ والمهمة تحمل رابط parliament الأصلي ← doc_id
مستقر ← عند زحف الأصل المؤرششف (طبقة 1، المسار التالي) تُطلق آلية
الترقية «استُبدلت بنسخة أفضل» تلقائياً. الصفوف الفارغة (63) تُهمل
وتبقى بفهرس اكتشاف `output/hf_index.json` لزحف الأرشيف.

**النتيجة المقيسة حياً (الصندوق):** 99 حُفظ | 2 بديل (dedup حسم لهوية
موجودة أفضل) | 9 مراجعة (دون بوابة الجودة) | 1 فشل | 63 فارغ.
القاعدة: **19 ← 120 وثيقة | 3,135 ← 11,191 مادة**. الإحالات:
**3 ← 56**. الحالات بعد rebuild: **ملغى 3 | معدَّل 5 | ساري 62**.
أبرز الوافدين: عقوبات (نسخة parliament بـ821 مادة!)، أصول المحاكمات
562، أصول الجزائية 491، العمل 338، **الأحوال الشخصية 321**، السير 235،
العقوبات العسكري 178. 263 اختباراً ناجحة (3 جديدة).

**على المالك:** `git pull` ثم `pip install pyarrow` ثم
`python cli.py hf-import` ثم `python cli.py law-status --rebuild --list`.
المتوقع عنده: ~+99 وثيقة (مع فروق dedup بسيطة لنسخ منتدياته) ←
~180 وثيقة | ~15,000 مادة، وقفزة إحالات مماثلة.

**التالي (المسار الثاني من قرار ج):** محرك زحف الأصول المؤرشفة عبر
Wayback (طبقة 1) — 174 رابطاً بالفهرس جاهزة؛ يُبنى عند عودة أرشيف
الإنترنت (مطفأ وقت البناء) — وكل أصل يُزحف يستبدل نظيره المتبنى
تلقائياً بآلية الترقية.

### ٨-ز. المالك نفّذ قرار ج — القفزة الكبرى (2026-09-16 ليلاً)

`hf-import` عند المالك: **99 حُفظ | 3 بديل | 0 فشل | 9 مراجعة | 63 فارغ**
(طابق الصندوق). `law-status`: **75 إحالة** (كانت 22) والحالات:
**ملغى 7 | معدَّل 7 | ساري 89** — وسلسلة العقوبات صارت **تعديل ×4**،
و11 مستهدفاً «✓ موجود بالمتن» (148:1949، 33:2005، 24:2003، 38:2006،
51:2004، 91:1959، 188:1926، 20:1994، 37:2009، 50:2001، 1:2011).
قاعدة المالك ≈ **181 وثيقة / ~15,000 مادة** — من 62/4,022 قبل ف١-ب
بأسبوع واحد. الهدف 100k: 15٪ بعد.

### ٨-ح. استطلاع ف٢-ج — syria-law.com (الطبقة المجتمعية، التالية بالترتيب)

ف٢ الكبرى كلها محجوبة خارجياً (pministry معلق عالمياً، Wayback مطفأ،
cb.gov.sy مؤجل بقرار سابق) — فالقابل للتنفيذ هو التالي بالترتيب.
القياس الحي: ووردبريس/Yoast **بنمط moj نفسه** (sitemap_index.xml)؛
robots متساهل (Disallow: /wp-admin/ فقط). الحجم: خرائط laws ‏82 +
splaws ‏26 (≈21 ألف صفحة قوانين محتملة) + موسوعة أنس كيلاني (اجتهادات
قانونية كصفحات قصيرة — ستصفيها بوابات الجودة ذاتها). **اختبار
المستخرج حياً:** صفحة «مرسوم عفو رقم 2 لعام 1992» → 4,256 حرفاً،
20 مادة، هوية المرسوم:2:1992 التقطت، نص نظيف يبدأ ب«المادة 1» —
المحرك شبه جاهز بآلية seed_moj نفسها (فهرس sitemap ← مهام topic).
الزحف الواسع (ساعات بفواصل مهذبة) على جهاز المالك حصراً بدستور
التوسعة. القرار معروض على المالك.

### ٨-ط. ف٢-ج — محرك syria-law.com مبني ومقيس حياً (موافقة المالك «ابنِ الآن»)

**البناء:** `community_seed.py` + أمر `cli seed-community` + 3 اختبارات.
ووردبريس/Yoast بنمط moj: فهرس sitemap_index ← خرائط القوانين (laws 82
+ splaws 26) ← مهام topic. **ترشيح المقتطفات من البذر ذاته أدباً:**
قياس الزحف الحي كشف أن 57٪ من الروابط (12,229 من 21,419) مقتطفات
أحادية بعنوان «(ال)مادة-… من قانون…» كلها 1-2 مادة تعرف بوابات الجودة
سلفاً أنها سترفضها — لا نحمّل الموقع ما سيُرفض. خرائط موسوعة
الاجتهادات (328 خريطة ≈ 65 ألف صفحة موسوعية قصيرة) مستبعدة أصلاً.

**التحقق الحي (الصندوق):** بذر 21,419 ← زحف 15 صفحة: **11 قانوناً
حقيقياً حُفظ** (سلسلة قوانين العفو 1971-1999 كاملة: عفو 6/1985،
11/1988، 2/1992، 18/1995، 3/1999، 44/1971، 26/1978، 26/1980…)،
و3 مقتطفات رُفضت بالبوابات، و1 فشل صريح — البوابات تميز بدقة.
بعد الترشيح: **9,188 رابطاً جوهرياً** بالطابور (فصول كاملة 908
تبقى — البوابات تحكم فيها).

**الزحف الواسع عند المالك (بدستور التوسعة — جهازه حصراً):**
```
git pull
python cli.py seed-community
python cli.py crawl --mode limited --pages 10000
python cli.py law-status --rebuild
```
~9,200 صفحة × ~3 ثوانٍ ≈ 8 ساعات — يُشغَّل ليلاً؛ الطابور الدائم
يستأنف أي انقطاع. المتوقع: +آلاف الوثائق المجتمعية (طبقة 4 — خلف كل
نسخة رسمية بآلية dedup) وقفزة إحالات وحالات جديدة.

### ٨-ي. سباق الجودة بالتوازي مع زحف syria-law (2026-09-17)

عملٌ بالموازاة أثناء الزحف الواسع عند المالك — قطعتا جودة من قياس HF:

**١. هوية بصيغ أرشيف مجلس الشعب:** قياس الـ174 عنواناً كشف 62 بلا هوية؛
الصيغ القابلة للإصلاح: «رقم/ 148/ لعام /1949/» (شرطة تسبق السنة — صف
العقوبات 760 مادة!) و«المرسوم 98/1961» (مائلة بلا كلمة رقم — السلطة
القضائية). الإصلاح: `لعام\s*/?\s*` + احتياط `LAW_ID_SLASH_RE` على
العنوان حصراً (النص يحمل إحالات صليبية فتكون هوية زائفة — اختبار يحرس
ذلك)، بنافذة {0,20} مقيسة بدقة (تشمل «مخالفات الأبنية 1/2003» وتستبعد
التقاط «قانون» البعيدة عن الرقم). النتيجة: 112 ← **116** هوية. ملاحظة
صدق: عقوبات HF تكسب «القانون:148:1949» (نوعها بعنوانها) فلا تتصادم
مع «المرسوم التشريعي:148:1949» — الدمج بقي لميزان وقت الحفط حيث عمل.

**٢. دمج تصادمات ما قبل الهوية — `cli dedup-existing`:** وثائق حُفظت
بلا هوية ثم كسبتها (--reidentify) لم تمر بميزان وقت الحفظ قط. لكل
هوية بأكثر من صف نشط: منافسة بالترتيب بنفس compare_candidates (تير ←
اكتمال ← جودة ← مواد...)، الخاسر superseded + نسخة بdocument_versions
+ قرار مدوَّن بdedup_decisions — نسخ لا حذف، والمواد تبقى بالتاريخ.

**العرض الحي (الصندوق):** reidentify gained 4 ← إحالات 56←63 ←
dedup-existing: تصادم واحد (نسختا السلطة القضائية 98:1961 المتطابقتان)
حُسم «تعادل — الأقدم تبقى» وأُرشفت الثانية. 271 اختباراً ناجحة.

**على المالك بعد انتهاء الزحف:** `git pull` ثم
`python cli.py law-status --reidentify --rebuild` ثم
`python cli.py dedup-existing` — المتوقع: هويات إضافية لصفوف syria-law
المحفوظة بلا هوية + دمج تصادماتها (عفو 1985 وغيرها مكررة بين HF
وسيريا-لو).

### ٨-ك. محرك Wayback مبني بالتوازي مع زحف syria-law (بانتظار عودة الأرشيف)

**البناء (قرار «ج»، مساره الثاني):** `wayback_source.py` + أمر
`cli wayback-crawl` + 5 اختبارات محلية بلا شبكة (§8.8 — الأرشيف كان
مطفأ وقت البناء). الوصفة: CDX (فلتر 200 + collapse=digest) ← أحدث لقطة
← جلبها **خاماً بلاحقة `id_`** (بلا شريط الأرشيف المحقون). الأدب كامل:
robots + تأخير fetcher لكل طلب (CDX واللقطات).

**المفتاح المعماري:** `final_url` هو **الرابط الأصلي** لا رابط الأرشيف —
فdoc_id يستقر على الرابط الرسمي، وكل صف متبنّى من HF (تير 3) يحمل نفس
الdoc_id فيُرقّى في مكانه تلقائياً «♻️ استُبدلت بنسخة أفضل» إلى تير 1
عند نجاح استدراج الأصل — **اختبار التاج يثبت ذلك كاملاً**: صف متبنّى
تير 3 ← تشغيل wayback-crawl ← نفس الصف تير 1 active + القديم مؤرشف
بdocument_versions ومواد جديدة.

**النطاق:** أهدافه قراءة من laws.parquet (روابط parliament/jus الأصلية،
174)؛ روابط PDF مؤجلة (تحتاج مسار PDF — لاحقاً إن لزم). المعايرة
الحية أول ما يعود أرشيف الإنترنت: `python cli.py wayback-crawl --limit 5`
أولاً ثم الباقي.

### ٨-ل. حادثة ليلة syria-law — الواقيتان اللتان بنتها الصباح (2026-09-17)

**ما جرى (دورة #28 عند المالك):** صفحات 7,140 | وثائق 193 | مواد 1,266
| **إخفاقات 6,778**. التحليل بالأرقام: أول ~362 صفحة سليمة (193 وثيقة
+ مراجعات)، ثم رفض الموقع كل ما تبقى — ومسار الفشل بـ`continue` كان
**يقفز فوق time.sleep(1.6)** فطُرِق المصدر بلا حد (~2 طلب/ث لساعة):
خرق أدب مزدوج — من الموقع (حجب) ومنّا (موجة بلا تأخير).

**الواقيتان (278 اختباراً):**
1. **التأخير المهذب قبل الجلب لا بعده** — كل صفحة تنتظر مهما نتيجتها
   (اختبار: الفاشلان انتظرا [1.6, 1.6]).
2. **قاطع الدورة** (`CIRCUIT_BREAKER_CONSECUTIVE_FAILURES = 12`):
   12 إخفاق جلب متتالٍ يوقف الدورة والطابور محفوظ لاستئناف هادئ —
   لولاها الليلة لتوقفت عند 12 بدل 6,778 (اختبار: 15 مهمة ← 12 فاشلة
   و3 بالطابور). + سجل requeue مختصر للكميات (أول 10 + «وN أخرى»).

**نتائج الصباح عند المالك رغم الحادثة:** 513 وثيقة | 16,930 مادة (17٪
من الهدف) | هويات gained 70 | إحالات 93 | **ملغى 24 | معدَّل 7 |
ساري 191** | dedup-existing: 18 تصادماً أرشفت 64 خاسراً.

**الاستئناف عند المالك:** بعد `git pull` — انتظار هدوء حجب الموقع
(ساعات)، ثم `requeue --status failed --contains syria-law` ثم
`crawl` — القاطع يحمي الآن: إن كان الحجب قائماً تتوقف الدورة عند 12.

### ٨-م. حكم القياس: syria-law محصودة — لا استئناف زحف (2026-09-17)

قياس عشوائي على مجموعتين من الخرائط المتأخرة (مفلترة بفلتر البذر نفسه،
35 صفحة عبر الأنبوب كاملاً من الصندوق): **~2/27 فقط تجتاز البوابات**
وحتى الناجحون مقتطفات 1-3 مواد («الأعمال التحضيرية للقانون المدني»).
الراسبون: فصول/أبواب قوانينا **كاملة عندنا من HF** (السير، الأحوال
الشخصية، مجلس الدولة) وصفحات دعائية بدرجات 6-20. الاستنتاج: ليلة
الزحف الأولى أخذت القيمة الحقيقية (193 قانوناً كاملاً — سلسلة العفو
والقوانين المتنوعة)؛ المتبقي (~2,000 بطابور المالك + 8,501 فاشلة)
مردوده ≈ صفر.

**القرار:** لا requeue بعد رفع الحجب — الحجب نفسه صار غير ذي أهمية،
ولا يُرسل للموقع طلب إضافي (أدب + حماية للعلاقة). ف٢-ج تُغلق منتجة.
ملاحظة مستقبلية: «الأعمال التحضيرية للقانون المدني» (فقه تشريعي ثمين
لمكتب محاماة) موجودة بالموقع كسلسلة مقتطفات — حصادها الموجه يحتاج
مسار «سلسلة» يجمع الشمل، إن طُلب لاحقاً.
