# خطة التسليم — مشروع ميزان

هذه القائمة تربط خارطة الطريق بالتسليمات العملية. علامة `[x]` تعني أن وجود العنصر تحقق في المستودع الحالي، ولا تعني أن المرحلة الإنتاجية مكتملة.

## الحالة عند إنشاء الخطة

- [x] وجود `recon.py` للاستطلاع.
- [x] وجود `fetcher.py` للجلب.
- [x] وجود `extractor.py` v3.3 كنقطة بداية.
- [x] وجود `crawler.py` v2.1 كنموذج أولي.
- [x] وجود طبقة SQLite أولية.
- [ ] وجود PostgreSQL و`pgvector`.
- [ ] وجود Embeddings أو Semantic Search.
- [ ] وجود FastAPI.
- [ ] وجود RAG.
- [ ] وجود Flutter داخل هذا المستودع.
- [ ] توثيق رقم المواد المستخرجة من قاعدة تشغيلية.
- [x] قراءة دستور العمل الأصلي لمستودع ميزان ومواءمته (انظر `CONSTITUTION.md` المنقح).

---

## التسليم 0 — المطابقة

- [x] الحصول على صلاحية قراءة `lawyer-office2`.
- [x] تسجيل البنية الفعلية للمشروع.
- [x] تحديد Backend وFrontend وقاعدة البيانات.
- [x] قراءة دستور العمل الأصلي.
- [x] كتابة النسخة المنقحة الخاصة بهذا المستودع (`CONSTITUTION.md`).
- [x] توثيق قرار الدمج (مُوصى بالخيار الانتقالي — بانتظار اعتماد المالك).
- [x] توثيق القرارات المفتوحة وعدم حسمها بالافتراض.

**مخرج التسليم:** تقرير مطابقة + قرار معماري + دستور عمل منقح. ✅ مُسلَّم في `DELIVERY/DELIVERY-0-MATCHING-REPORT.md` بتاريخ 2026-09-05.

---

## دفعة P0 (2026-09-05) — أُنجزت قبل التسليم 1

- [x] إصلاح توليد معرف الوثيقة (`make_doc_id` مستقر بديل `doc_<timestamp>`).
- [x] منع تكرار الوثائق والمواد عند إعادة التشغيل (`save_document` idempotent).
- [x] تنفيذ robots.txt فعلياً (بديل الغلاف الذي كان يعيد True دائماً).
- [x] إزالة الكتلة المكررة في `config.py` + `RESPECT_ROBOTS = True`.
- [x] درجة قانونية محسوبة (بديل الثابت الوهمي 85.0).
- [x] اختبارات محلية بلا شبكة: `tests/test_p0_identity.py` (12 اختباراً — كلها ناجحة).

---

## التسليم 1 — أساس التطوير

- [x] إضافة `pyproject.toml` (مع console script `mizan-harvest` وإعدادات pytest).
- [x] إضافة `.env.example` (BING_API_KEY فقط — بلا أسرار).
- [x] تثبيت طريقة تشغيل موحدة (`cli.py`: init/stats/crawl/discover/seeds/sources).
- [x] إضافة logging موحد (`logging_setup.py`: طرفية UTF-8 + logs/harvester.log) وتحويل كل print.
- [x] إضافة CLI (أعلاه) مع أوضاع dry/limited/full(--yes).
- [x] فصل اختبارات الشبكة (marker `network` + addopts يستبعدها؛ 25 محلية ناجحة).
- [x] إضافة CI (`.github/workflows/ci.yml`: اختبارات + CLI smoke + GUI smoke offscreen).
- [x] إضافة Fixtures HTML عربية (tests/fixtures: قانوني phpBB، غير قانوني، نتائج بحث، robots عريض).

**مخرج التسليم:** بيئة جديدة قابلة للتشغيل واختبارات محلية ثابتة. ✅ سُلم 2026-09-05 — تحقق حي: زحف limited حفظ 1 وثيقة/5 مواد ثم أعيد التشغيل فتخطاها بلا تكرار.

---

## التسليم 2 — Extractor v4

- [x] تنظيف Unicode والمسافات والالتصاق (normalize_unicode مستقلة عن علة v3).
- [x] اختيار حاوية المحتوى بمؤشر قابل للتفسير (selector_info + سقوط عام).
- [x] دعم الأرقام العربية والهندية (+ الفارسية).
- [x] دعم الصياغات اللفظية للمادة (حتى 99).
- [x] استخراج المقدمة كمادة أولى بشروط واضحة (is_preamble، لا قسر).
- [x] استخراج الكتاب/الباب/الفصل/المبحث/المطلب متراكمة.
- [x] دعم المكررة والمعدلة (is_duplicate / amendment_note، لا إسقاط صامت).
- [x] حفظ الفقرات ومحدد المصدر (paragraphs + source_locator، ومخزنة فعلياً في articles).
- [x] حساب quality_score بأسباب + خصم عند غياب المواد.
- [x] إخراج عقد JSON موحد (article_key + حقول §6.2).
- [x] 14 اختبار v4 محلياً + تقرير مقارنة حي: DELIVERY/EXTRACTOR-V4-REPORT.md.

**مخرج التسليم:** Extractor v4 مع تقرير مقارنة على Fixtures. ✅ سُلم 2026-09-05 ومعتمد في crawler v2.3 (v4 ≥ v3 في 3 صفحات حية و6 Fixtures).

---

## التسليم 3 — الزحف القابل للاستئناف

- [x] Queue دائمة في قاعدة البيانات (`crawl_tasks` + `crawl_queue.py`).
- [x] تطبيع الروابط (`urls.py`؛ معاملات التتبع خارج و`start` يبقى للـ pagination).
- [x] دعم pagination (روابط `start=` تُدرج مهام أقسام تالية).
- [x] تطبيق robots.txt فعلياً. (أُنجز مبكراً في دفعة P0 — 2026-09-05)
- [x] إدارة retries حسب الخطأ (`classify_error`: block/fail/retry + إعادة طابور بعدّاد محاولات).
- [x] حفظ snapshots خارج Git (`data/snapshots` بصمات sha256).
- [x] منع التكرار عند إعادة التشغيل (مثبت حياً: دورة 2 أكملت الطابور وتخطت المحفوظ).
- [x] أوضاع dry/limited/full (التسليم 1).
- [x] تقرير لكل دورة (`crawl_runs` + `output/run_N.md` + أمر `cli runs`).

**مخرج التسليم:** دورة زحف يمكن إيقافها واستئنافها دون تكرار. ✅ سُلم 2026-09-05 بتحقق حي (دورتان متتاليتان).

---

## التسليم 4 — طبقة البيانات

- [ ] تصميم النموذج النهائي.
- [ ] إضافة migrations.
- [ ] إضافة PostgreSQL محلياً.
- [ ] نماذج الوثائق والإصدارات.
- [ ] نماذج المواد والإصدارات.
- [ ] نماذج hierarchy.
- [ ] نماذج crawl runs/tasks.
- [ ] Importer من SQLite القديمة.
- [ ] تقرير مطابقة قبل/بعد النقل.
- [ ] خطة rollback ونسخة احتياطية.

**مخرج التسليم:** PostgreSQL قابلة لإعادة البناء من migrations وبيانات مستوردة قابلة للتدقيق.

---

## التسليم 5 — البحث الدلالي

- [ ] سياسة chunking.
- [ ] اختيار نموذج embedding وتسجيل إصداره.
- [ ] جدول embeddings مرتبط بإصدارات المواد.
- [ ] بحث نصي.
- [ ] بحث vector.
- [ ] Hybrid Search.
- [ ] مجموعة أسئلة عربية للتقييم.
- [ ] تقرير جودة الاسترجاع.

**مخرج التسليم:** نتائج بحث يمكن ربطها بالمادة والوثيقة والمصدر.

---

## التسليم 6 — API وRAG

- [ ] API للوثائق والمواد.
- [ ] API للبحث.
- [ ] API إداري لحالة الزحف.
- [ ] مصادقة وتفويض.
- [ ] OpenAPI.
- [ ] استرجاع chunks موثوقة.
- [ ] إجابات مع استشهادات.
- [ ] رفض آمن عند ضعف المصدر.
- [ ] تقييم عربي للأسئلة والأجوبة.

**مخرج التسليم:** Backend تجريبي لا يقدم جواباً قانونياً دون مصدر قابل للتتبع.

---

## التسليم 7 — Flutter

- [ ] مكتبة الوثائق.
- [ ] عرض الهيكل الهرمي.
- [ ] البحث النصي والدلالي.
- [ ] الشات القانوني مع الاستشهادات.
- [ ] النماذج.
- [ ] حالات الخطأ وعدم كفاية المصدر.
- [ ] تكامل المصادقة والصلاحيات.

**مخرج التسليم:** واجهة ميزان مبنية على API مستقرة وعقد بيانات موثق.

---

## التسليم 8 — Exporter: حزمة ميزان

> أُضيف 2026-09-05 بناءً على طلب المالك تحويل الزاحف إلى أداة تصدر حزمها لميزان (التصميم: `DELIVERY/DESIGN-TOOL-APP.md` §5).

- [ ] توليد `laws_decrees_index.csv` بأعمدة ميزان الحالية حرفياً.
- [ ] توليد ملفات النصوص (slug لاتيني + عنوان عربي في الفهرس).
- [ ] توليد `manifest.json` (إصدارات + أعداد + بصمات).
- [ ] توليد `REPORT.md` (ماذا/متى/كم/الفشل + إخلاء مسؤولية).
- [ ] بوابة تحقق: sha256/size مطابقة، لا id مكرر، المجتاز فقط افتراضياً.
- [ ] اختبار محلي: حزمة مولدة تجتاز بوابات التحقق.

**مخرج التسليم:** حزمة ميزان قابلة للنسخ، تجتاز التحقق آلياً.

---

## التسليم 9 — الأداة والتغليف (تطبيق Windows)

- [~] قشرة واجهة PySide6 (محسوم بقرار المالك): نموذج أولي 5 شاشات بهوية ميزان + لقطات `docs/screenshots/` — بانتظار ربط النواة الحقيقية.
- [ ] شاشات: النطاق، التشغيل، المكتبة/المراجعة، التصدير، الإعدادات.
- [ ] بناء PyInstaller (نمط المجلد) على Windows.
- [ ] مثبّت Inno Setup + أيقونة + معلومات إصدار.
- [ ] CI: وظيفة بناء exe على windows-latest.
- [ ] دليل مستخدم قصير (تشغيل، زحف، تصدير، نسخ الحزمة لميزان).
- [ ] إصدار v1.0 موزع.

**مخرج التسليم:** تطبيق Windows يعمل دون Python وينتج حزمة ميزان.

---

## التسليم 10 — استكشاف المصادر (الزاحف يبحث عن مصادره)

> أُضيف 2026-09-05 بطلب المالك. النواة منفذة ومختبرة (12 اختباراً في tests/test_discovery.py).

- [x] ثلاث قنوات: بحث DuckDuckGo بلا مفتاح (مع معالجة صريحة لصدّ الروبوت)، Bing بمفتاح `.env`، إدراج يدوي + دليل بذور مرفق.
- [x] بوابة تقييم موحدة: جلب مهذب يحترم robots ← استخراج ← `legal_score` ← كشف المحرك (phpBB/WordPress/عام) ← حكم بأسباب.
- [x] جدول `sources` في المخطط: `proposed/approved/rejected` — لا زحف قبل موافقة صريحة.
- [x] سجل idempotent بمفتاح `sha256` للرابط المطبَّع.
- [x] شاشة «استكشاف المصادر» في النموذج الأولي (الشاشة 0).
- [ ] ربط الشاشة بالنواة الحقيقية (بعد التسليم 1).
- [ ] توسيع دليل البذور بمصادر رسمية موثقة بقرار المالك.

**مخرج التسليم:** نواة استكشاف مختبرة محلياً + واجهة نموذجية.

---

## بوابة اعتماد أي مرحلة

لا تنتقل المرحلة إلى التنفيذ الواسع إلا إذا توفر:

- [ ] كود قابل للتشغيل.
- [ ] اختبار مناسب.
- [ ] توثيق.
- [ ] تقرير نتائج.
- [ ] عدم وجود أسرار في Git.
- [ ] إمكانية التراجع عن تغييرات البيانات.
- [ ] مصدر واضح لكل رقم أو ادعاء متعلق بالبيانات.
